# Countdown Timer

A simple countdown timer web app built with FastAPI.

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
python -m uvicorn main:app --reload
```

Opens at [http://localhost:8000](http://localhost:8000)
